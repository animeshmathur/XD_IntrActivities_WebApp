import { FilterBirthdayByPipe } from './filter-birthday-by.pipe';

describe('FilterBirthdayByPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBirthdayByPipe();
    expect(pipe).toBeTruthy();
  });
});
