import { FilterWorkAnniversaryByPipe } from './filter-work-anniversary-by.pipe';

describe('FilterWorkAnniversaryByPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWorkAnniversaryByPipe();
    expect(pipe).toBeTruthy();
  });
});
